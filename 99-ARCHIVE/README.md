# Archive

This folder is reserved for material that is no longer on the supported runtime path.

The existing `../legacy/` directory contains older demo/compatibility material. It stays separate until every historical reference is fully verified, so cleanup does not accidentally remove something still used.
