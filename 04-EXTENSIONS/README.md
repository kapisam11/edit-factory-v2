# Extensions and resources

Optional or content-oriented resources live here:

- `../prompt_templates/` — reusable prompt and report resources
- `../knowledge_base_v2/` — learning/feedback data used by the application
- `../hooks.weights.json` — hook scoring weights

These resources support the application but are separate from its main Python implementation.
